{
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "cors": "^2.8.5"
    },
    "dependencies": {
        "moment": "^2.30.1",
        "react-hot-toast": "^2.5.2"
    }
}
